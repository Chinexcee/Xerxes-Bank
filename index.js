// Optional Chaining (?.) Used to access deeply nested properties without worrying about undefined errors
// let user = {name: 'chinedu'};
// console.log(user?.name?.);

// Nullish Coalescing(??) provides a fallback only when the value is null or undefined

